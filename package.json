{
  "name": "burst-rights-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Burst — music licensing, rights-attribution & royalty engine",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:watch": "npm run test:watch --workspace @burst/domain"
  }
}
