{
  "name": "@burst/domain",
  "private": true,
  "version": "0.1.0",
  "description": "Pure, deterministic domain core: money, splits, attribution, ledger math",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.23.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
